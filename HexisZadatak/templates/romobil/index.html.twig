{% extends 'base.html.twig' %}

{% block body %}
  <h1 style="text-align: center">Evidencija romobila</h1>

  <div class="container">
  <div class="card">
    <div class="card-body">
      <div class="row" style="margin: 10px">
        <button class="btn btn-success"><a href="{{path('romobil.create')}}">Unos novog vozila</a></button>
      </div>
    </div>
  </div>

  {% for message in app.flashes('success') %}
    <div class="alert alert-success">
      {{message}}
    </div>
  {% endfor %}

  <table class="table table-striped">
      <thead>
        <tr>
          <td>Tip</td>
          <td>Broj putnika</td>
          <td>Marka</td>
          <td>Status</td>
          <td>Action</td>
        </tr>
      </thead>
      <tbody>
        {% for romobil in romobili %}
          <tr>
            <td>{{romobil.tip}}</td>
            <td>{{romobil.brojPutnika}}</td>
            <td>{{romobil.marka}}</td>
            <td>
              {% if romobil.status == true %}
                Zauzet
              {% else %}
                Slobodan
              {% endif %}
            </td>
            <td><a href="{{path('romobil.update', {id: romobil.id})}}">uredi</a></td>
            <td><a href="{{path('romobil.delete', {id: romobil.id})}}">obriši</a></td>
          </tr>
        {% endfor %}
      </tbody>
  </table>

    <button class="btn btn-success"><a href="{{path('home')}}">Natrag</a></button>
  </div>
{% endblock %}
